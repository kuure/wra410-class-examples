# search-ahead

This is an example of "search ahead", where matches to what you are typing
fill the text box as suggestions. 

For this to work, you _must_ run it through a server, like node or the
live server plugin in VS Code
