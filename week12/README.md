# Week 12

## 11/11/2024 - 11/13/2024


### Monday

Simple node demo - I feel obligated to talk about node, and the
`node-express` folder contains an example of JS code to run from the
command line as well as a simple HTTP server.


The `class-manipuluation` folder is a template for setting up CSS and JS
that changes the classList attribute to modify the appearance of an
element.


### Wednesday

The `class-manipulation` code is complete and functional; this version is
commented and explains how things work.

The `react-demo` is a simple example of a React application with two
components.
